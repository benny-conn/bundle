FROM golang
WORKDIR /bundle/db
COPY . .
ENV DATABASE_PORT=8040
ENV REPO_PORT=8060
ENV WEB_PORT=8080
ENV API_PORT=8020
ENV API_HOST=api
ENV WEB_HOST=web
ENV REPO_HOST=repo
ENV DATABASE_HOST=db
RUN go install cmd/db/db.go
EXPOSE 8040
CMD ["db"]